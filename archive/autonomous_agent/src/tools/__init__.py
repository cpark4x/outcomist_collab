"""
Tool implementations for the Executor Agent.

Each tool module provides an `execute()` function that:
- Takes inputs dict and WorkingMemory
- Performs the tool's operation
- Returns result dict with content and metadata
"""
